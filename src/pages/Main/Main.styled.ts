import styled from '@emotion/styled';

const Container = styled.div`
	display: inline-flex;
	flex-direction: column;
	row-gap: 30px;
	padding: 20px;
`;

const Title = styled.h1`
	font-weight: 800;
`;

const JoinBtn = styled.button`
	height: 30px;
`;

export { Container, Title, JoinBtn };
